#
# The modules is needed in project
# to run easily

python3.8                        # my version (no need)
opencv-python==4.5.2.52          # pip3 install opencv-python
Pillow==8.2.0                    # pip3 install Pillow
face_recognition==1.3.0          # pip3 install face_recognition
pygame==2.0.1                    # pip3 install pygame
numpy==1.20.3                    # pip3 install numpy